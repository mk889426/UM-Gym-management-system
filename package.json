{
  "name": "gms",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --production=false && npm run build && cd ../server && npm install",
    "start": "cd server && node server.js"
  }
}
